{
  "identifier": "default",
  "description": "默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:app:allow-set-app-theme",
    "shell:allow-open"
  ]
}
